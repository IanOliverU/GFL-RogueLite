# Milestones and implementation status

## Authoritative status

Baseline inspected on 2026-09-06. Existing documents preserved; no Git repository was present in the project or enclosing workspace.

Current application root: `C:\Users\MY PC\Desktop\GFL Game`. The existing M0/M1 project was relocated from the nested starter folder on 2026-09-06. Original implementation evidence below is retained as history; relocation verification is recorded separately.

| Milestone | Status | Evidence | Director feedback |
| --- | --- | --- | --- |
| M0 Foundation | Technically complete | Build/typecheck/lint passed; dev starts; production minimal scene inspected in headless Chrome | Pending Ian's review |
| M1 Movement and aiming | Technically complete | Build/typecheck/lint; 13 unit tests; 3 production browser tests; screenshots inspected | Pending Ian's review |
| M2 Combat — expanded six-doll scope | Accepted and committed | `e8bb217`; previous 38 unit / 10 browser checks retained | Accepted by Ian in the M3 authorization |
| M3 Evasion and pressure | Accepted and committed | `cbafbdc`; 66 unit / 12 browser checks at core completion, 71 / 13 with follow-up | Core mechanics and pause-menu/timer follow-up accepted by Ian after manual playtest (incl. real Alt-Tab) |
| M4 Run progression | In progress | XP/level-up implementation and verification active | Acceptance pending Ian's review |
| M5 Visual prototype | In progress | First reusable checkpoint kit integrated; acceptance pending | No director acceptance recorded |
| M6 First playable | Planned | None | Not recorded |

Ian accepted M2 and confirmed it committed, then authorized M3 evasion and enemy pressure. M3 is the current stopping point; its acceptance remains pending. Mark one milestone In progress at a time. Completion means criteria have evidence; director feedback is a separate field and must not be invented. Record unperformed checks as pending. No automatic deployment or release follows completion.

## M0 — Foundation

Inspect directory/Git; preserve docs; scaffold compatible Vite/React/TS/R3F/Three dependencies. Add startup/build/typecheck/lint scripts; record Node and dependency versions. Render a minimal scene and confirm the production build. Keep code structure small.

Exit: local app starts, minimal scene is visible when browser testing is available, build/typecheck/lint pass. Missing browser access must be recorded, not counted as passed.

## M1 — Movement and aiming

Fixed elevated orthographic camera, flat bounded arena, foot-anchored character placeholder, WASD, normalized diagonals, cursor-ground projection, aim indicator, pause/focus/resize handling. Optionally validate one sprite and one simple prop import; placeholders remain valid.

Exit: all-around aim works while moving and after resizing; camera translation does not shift aim; boundaries work; UI/focus clears input; no background simulation. No enemies or weapon damage in this milestone.

## M2 — Combat

Approved scope update (2026-09-06): implement character selection for Sabrina, Qiongjiu, Tololo, Mosin-Nagant, Peritya and Vepley, each with a distinct data-driven basic weapon. This supersedes Sabrina-only M2 and the later basic-roster rollout. Shared combat includes fire-range gating, volley/burst timing, magazines/reload, swept projectile collision, piercing, knockback, a pursuing enemy type, health, hit feedback, death, retry with the same doll and return to selection. Gun fires along cursor aim, never snaps to a target. Provide a static obstacle to verify wall hits. Use clearly labeled original placeholders and prototype fan-game adaptations, not claimed official kits.

Exit: all six can be selected, start, fire distinct weapons, damage/defeat enemies, die and restart. Test bursts, piercing, knockback, reload, pause/focus and complete run-state resets when retrying or switching dolls. Hits/misses/piercing match visuals; damage stops after death; restarts do not duplicate listeners/entities; firing is frame-rate independent. Three-skill kits, ultimates, evolutions, polished animation and M3+ systems remain excluded. Stop after expanded M2; acceptance stays pending director review.

## M3 — Evasion and pressure

Dash movement/cooldown/invulnerability; collision prevents wall crossing. Add ranged enemy with readable telegraph/projectiles and initial spawn pacing. Keep concurrent counts modest.

Exit: dash can avoid a demonstrated attack during its invulnerability interval; cooldown prevents spam; ranged attacks are avoidable; no unavoidable spawn directly on the player.

## M4 — Progression

XP pickups, queued level-ups, three eligible choices, limited equipment and gun ranks, one automatic Sabrina skill, one evolution and elite reward opportunity. Add development controls for testing progression.

Exit: UI pauses simulation, caps/eligibility/fallbacks work, evolution is reachable naturally within the tuned run, and new runs reset in-run state. Development shortcuts are not production progression.

## M5 — Visual prototype

Build the checkpoint from modular assets. Integrate consistent idle/run/fire/hit/death presentation (simple procedural hit effects are acceptable and identified). Add restrained lighting, combat audio, effects and quality controls. Audit asset provenance.

Exit: art direction is reviewable, bullets/hazards remain readable, cursor and muzzle align, and measured performance on the named machine is documented. No claim of final animation quality based only on generated frames.

## M6 — First playable

Three-to-five-minute encounter, final elite, win/death/results, menu, settings, local unlock/save behavior, loading and failure states. Browser smoke test in available target browsers. Public hosting remains separately requested.

Exit: an independent tester can start, fight, upgrade, evolve when requirements are reached, win/die, and restart; save errors do not prevent play. Record browser coverage and unresolved limitations. Initial playable availability does not imply six dolls are complete.

## Later expansion

The six basic weapons and selection are now authorized in M2. Full three-skill kits, more equipment, roughly ten-minute pacing and additional arenas remain later work after feedback. Room-based structure/mobile/online features require an explicit scope update.

## Evidence log template

### M0 and M1 evidence — 2026-09-06

- Workspace: `GFL2-Roguelite-Starter` inside `GFL Game`. All baseline documents read before edits. `git status --short --branch` reported no repository in either folder, both before and after implementation. Existing design documents preserved; status/setup/provenance updated. No commit, push or deployment performed.
- M0 completed before M1: compatible npm packages and one lockfile installed; Node 24.12.0, npm 11.7.0. Minimal scene production build/typecheck/lint passed. `npm run dev -- --port 5173 --strictPort` started successfully. Initial production Chrome smoke test passed; its screenshot was inspected and showed the floor and upright placeholder before M1 work began.
- M1 changes: pure 60 Hz simulation with at most six catch-up steps; 6 units/second normalized movement; 24 × 24 arena with radius-aware bounds; foot-anchored upright flat geometry; fixed camera angle with 45% translation follow; canvas-relative ground projection after camera updates; shared aim direction and target ring; pause, focus/visibility input clearing, deliberate resume, listener cleanup and graphics failure handling. Only playing/paused states are implemented; later run states and content systems remain deferred.
- Checks performed: `npm run build` (includes `npm run typecheck`) passed; standalone `npm run typecheck` and `npm run lint` passed. `npm run test`: 13 tests passed in 3 files. `npm run test:browser`: 3 production browser tests passed. `npm ls --depth=0` verified installed versions; npm install reported zero known vulnerabilities at install time.
- Unit coverage: equal straight/diagonal movement, opposing keys, all bounds and edge sliding, render-rate-independent movement at 30/60/144 Hz, pause/reset accumulator, bounded catch-up, invalid deltas, subscription cleanup, canvas offsets, invalid/parallel/behind-camera rays, near-zero direction retention, all-quadrant projection across resize/camera translation and stationary-pointer reprojection.
- Browser: installed Google Chrome 152.0.7977.77, headless Playwright 1.63.0, software WebGL (SwiftShader), DPR 1. Windows x64 10.0.26200, AMD Ryzen 5 7500F. No reference-machine GPU/frame-time performance measurement was made. Connected browser-control tool reported no browser, but local Chrome automation worked.
- Browser actions actually performed: cursor moved into four quadrants; D movement with stationary cursor until X > 3; verified translated camera and changed floor target; cursor projection error below 0.1 CSS pixel; resized viewport from 1280 × 800 to 960 × 700 without moving the mouse; reached X=11.6 boundary; moved with W/S; clicked Pause and Resume; used Escape both ways; confirmed frozen simulation time and empty held inputs; dispatched synthetic blur/focus browser events and confirmed no automatic resume or movement drift. Screenshots `test-results/m1-moving-aim.png` and `test-results/m1-resized.png` were visually inspected. They are ignored generated artifacts, recreated by the browser suite.
- Limitations: focus-loss automation used a synthetic DOM blur event, not physical OS Alt-Tab. Real tab switching/minimizing, browser zoom/DPR changes, actual graphics context recovery, other browsers, human input feel and high-refresh smoothness remain director checks. Latest fixed-step positions are rendered without interpolation. The ~1.08 MB minified / ~298 KB gzip JavaScript chunk triggers Vite's bundle-size warning; build succeeds. No enemies/projectiles exist and no 60 FPS claim is made.
- Director feedback: not received; acceptance remains pending. The manual checklist is intentionally unchecked. Next action: Ian's M1 movement/aiming playtest. M2 requires subsequent authorization.
- Suggested commit message, if Git is initialized later: `feat: implement M0 foundation and M1 movement playground`.

### Project relocation verification — 2026-09-06

- Scope: relocation and verification only. Current application root is `C:\Users\MY PC\Desktop\GFL Game`. The original nested `GFL2-Roguelite-Starter` directory is no longer present. M0/M1 implementation and tests were not rebuilt or changed; M2 remains unstarted.
- Inspection: both roots and hidden/dotfiles inspected before moving. The intended parent root was accessible and contained only the starter folder, so there were no destination conflicts or overwritten files. Neither root nor the project tree contained a Git repository; there was no Git history to transfer. No repository was initialized, and no commit, push or deployment was performed.
- Servers: stopped the old root's Vite dev server (5173), production preview (4173), and stale Playwright runner before the move. Verified both ports were no longer listening. PowerShell `Stop-Process` failed with an internal exception; the same identified processes were stopped successfully through the .NET process API.
- Transfer: all contents, including `.gitignore`, source, tests, docs, configuration, lockfile, dependencies and existing generated output, moved into the intended root without changing internal structure. A 9,017-file SHA-256 manifest was captured and verified during transfer. The old directory was removed only after checksums matched and it was confirmed empty. After the usage-limit interruption, verified all 9,017 files still existed with matching sizes and rechecked SHA-256 for all 44 non-dependency files before documentation edits or rebuilds.
- Preservation: after the checks, SHA-256 comparisons confirmed all 29 application/test/package/configuration files were unchanged. Prior browser captures and the manifest remain in ignored `.tmp/relocation-before/`; the original M0/M1 evidence above is retained. Only README, START_HERE, and location/status entries in the documentation were edited. The original location decision in DECISIONS is explicitly superseded.
- Corrected-root results: `npm run build` passed (including its typecheck); standalone `npm run typecheck` passed; `npm run lint` passed; `npm run test` passed all 13 tests in 3 files; `npm run test:browser` passed all 3 existing production tests in 8.3 seconds and exited successfully. No requested check was left unrun. Existing moved dependencies worked; no reinstall, version or lockfile changes were needed. `dist/` was regenerated from the corrected root.
- Browser coverage: fresh production preview started by the existing Playwright configuration, installed headless Chrome 152.0.7977.77 with software WebGL. The unchanged tests covered all-around aiming, stationary cursor during movement/camera follow, resizing, boundaries, pause/resume and synthetic focus loss. These are automated checks, not new director playtest feedback.
- Limitations retained: the ~1.08 MB minified / ~298 KB gzip JavaScript bundle still triggers Vite's size warning. Real OS Alt-Tab/tab switching, browser zoom/DPR changes, other browsers, actual graphics context recovery, subjective movement feel and reference-GPU performance remain manual checks. Focus-loss testing still uses a synthetic event; no new performance or director-acceptance claim is made.
- Director acceptance: pending Ian's review. Next action: open the corrected root in VS Code, run `npm run dev`, and playtest M1. No further development is authorized by this relocation task.

### Expanded M2 implementation and verification — 2026-09-06

- Authorization: Ian explicitly expanded M2 from Sabrina-only combat to all six playable dolls and character selection, overriding the earlier basic-roster restriction. Full skills, ultimates, evolutions, polished animations and M3+ systems remain excluded. Acceptance is pending Ian's review.
- Starting state: application root `C:\Users\MY PC\Desktop\GFL Game`; Git now exists and was clean on `main` tracking `origin/main` at inspection. M0/M1 source and verification history retained. Existing package versions/lockfile reused without changes. No commits, push or deployment made.
- UI: six named, selectable cards show basic weapon descriptions, explicit placeholder labels and selected state. Start uses the chosen doll. Combat HUD shows doll/weapon, health, ammunition/reload and kills. Game over offers same-doll retry and return to selection. Shared static flat geometry uses six placeholder colors; all art is project-created.
- Combat: typed character/weapon/pursuer/obstacle data; one shared firing/burst/reload system; swept projectiles with sorted collision, enemy hit bookkeeping, bounded piercing and knockback; 40-HP pursuers with contact damage and navigation around one cover block. Up to 8 enemies and 192 projectiles; no wave/progression/XP system. M1 movement, foot anchor, camera and cursor aim are retained. Projection is sampled after movement before firing, and refreshed on idle render frames.
- Lifecycle: selection, playing, paused and game_over are authoritative. Pause/focus clears held inputs and aim; simulation and weapon/reload timers freeze. Escape/focus cannot bypass selection or revive game over. Retry/switch replaces health, ammo, all weapon/hurt/spawn timers, entities, IDs, aim/input, elapsed time and counters. No extra listeners or timers are created on retry; leaving the arena unbinds browser listeners. M1 no-combat route is available at `?mode=playground`; its browser assertions were preserved with only the entry URL adjusted.
- Checks: `npm run build`, standalone `npm run typecheck`, `npm run lint` and `git diff --check` passed. `npm run test`: 38 tests passed in 4 files (13 prior + 25 combat). `npm run test:browser`: all 10 tests passed in 1.8 minutes (6 doll lifecycles, 1 selection/switch lifecycle, 3 M1 regressions). Initial burst timing test needed floating-point tolerance rather than exact equality; the weapon timing itself matched the specified sequence. Final build retained the same production JS/CSS hashes as the full browser-tested build.
- Unit evidence: every doll fired, dealt damage, killed, died, froze on death and restarted/switched with a pristine world; every magazine/reload checked, including paused reloads. Verified Qiongjiu's 0.1-second burst spacing and 0.55-second recovery, current-step aim, mid-burst pause/no queued fire without fresh aim, range/aim gate cancellation, even shotgun spread and distinct damage/cadence, 30/60/144 Hz firing counts, projectile cap, swept thin-target/tangent/overlap hits, distance-ordered three-enemy sniper piercing, no repeat hits, cover/perimeter stops, non-additive stronger Vepley impulse, collision-constrained knockback, pursuit around cover and bounded spawns.
- Browser evidence: installed Chrome 152.0.7977.77, headless Playwright with software WebGL, Windows x64, Node 24.12.0/npm 11.7.0. For each doll: selected card/summary, started, aimed at the initial real pursuer, observed shots/hits/kills, paused during held movement, asserted frozen timers and empty inputs, dispatched synthetic blur/focus, deliberately resumed, left aim input to take contact damage, reached real game over, checked time remained frozen, retried and checked fresh HP/ammo/timers/projectiles/counters. Separate case returned to selection and switched Sabrina to Peritya. All recorded browser error arrays were empty. No simulated damage/death shortcut or mutable browser test control was introduced.
- Visual evidence: inspected selection screenshot, Peritya combat/HUD screenshot and the live Sabrina arena with pursuer/health bar/cover. A focused Sabrina browser rerun passed after adding the arena capture. Generated captures are ignored under `test-results/`. No claim of manual human playtesting or official character likeness.
- Known limitations/manual checks: provisional tuning; one enemy type; static placeholders; no enemy separation (pursuers can overlap); no audio or later systems. Software rendering is not a reference-GPU performance test. Physical Alt-Tab/tab visibility transitions, zoom/DPR, other browsers, real context recovery and subjective movement/weapon feel remain unverified manually. Reload/burst/piercing/knockback specifics are asserted by simulation tests; director comparison in the real browser remains pending. Vite still warns about the ~1.10 MB minified / ~302 KB gzip rendering bundle. Original M0/M1 and relocation limitations above remain historical evidence.
- Next action: Ian compares the six weapons using `docs/PLAYTEST_CHECKLIST.md` and supplies acceptance/tuning feedback. Stop after M2; do not begin M3 without authorization.

### M3 implementation and verification — 2026-09-06

- Authorization: Ian accepted M2 (`e8bb217`) and authorized M3 evasion and enemy pressure. Acceptance remains pending. Uncommitted/untracked M3 work was preserved and finished in place; no M4 systems added.
- Changes: dash data/tuning (`DASH`: 4 units over 0.20 s at speed 20, 1.20 s cooldown from activation, invulnerable while `dash.remaining > 0`); swept dash collision (`moveCircleSwept`) stopping at cover/perimeter; shared player-damage gate (contact + hostile shots share the 0.7 s hurt interval, dash blocks both); ranged enemy data (`RANGED`: 50 HP, 0.90 s warning locked at onset, 2.8 s recovery, range 10 / hold 7 / retreat 4, 7 u/s shots); warning path shared by telegraph rendering and shot direction; swept hostile projectiles with relative player-motion sweep and dash dodge counting; deterministic bounded enemy separation; phased spawn pressure (first spawn 4 s; phases 0/15/40 s; intervals 3.5/3.0/2.6 s; caps 4/6/8 alive; at most 1/2/2 ranged; 6-unit player exclusion, no banked spawns); Space input with scroll prevention and no repeat queuing; pause/focus clearing queued dash with resume-delta clamping; full M3 state resets; purple ranged placeholders, red warning stripes, orange hostile shots, dash trail/ring placeholders, dash HUD (READY/countdown/DASHING).
- Cooldown check: the from-activation timing was re-derived (activation step sets 1.20 s; each later step decrements once; 73 steps total reach exactly 0). The weak `> cooldown - 0.5` assertion was tightened to `closeTo(cooldown - 29 * FIXED_STEP)` after 30 steps, keeping the 73-step ready check. This confirms the timing rather than masking a defect.
- Test fix: the warning screenshot was captured while paused, so the pause dialog covered the arena. The browser test now captures `m3-warning.png` while playing, then re-acquires a fresh warning before the freeze assertion (the first warning can fire during the screenshot). No gameplay expectations were weakened.
- Checks performed by the finishing agent: `npm run typecheck` passed; `npm run lint` passed; `npm run test` 66 passed in 5 files; `npm run build` passed (~1.11 MB minified / ~304 KB gzip, size warning only); `npm run test:browser` 12/12 passed in ~2.6 min on the final code (6 doll lifecycles, selection/switch, 2 M3, 3 M1 regressions; headless Chrome, software WebGL). An intermediate full run exposed the warning-race above (11/12); after the test fix, M3 isolation (2/2) and the final full suite (12/12) passed.
- Screenshots inspected: `test-results/m3-warning.png` now shows the red beam through the player with the warning-tinted ranged enemy; `m3-projectile.png` shows the purple ranged enemy, an orange hostile shot in flight, and HP 80/100 (correct 100 maximum); `m3-dash.png` shows DASHING HUD state. Dash trail/ring placeholders are faint at capture scale; enemy health bars use per-kind maxima (`definition.health`) but are thin at game scale.
- Known limitations: provisional tuning; faint dash trail in stills; thin enemy health bars; focus-loss coverage still synthetic; real Alt-Tab, zoom/DPR, other browsers, context recovery, reference-GPU performance and human feel remain manual. No XP/upgrades/evolutions/polished assets added.
- Director feedback: Ian accepted the core M3 mechanics after manual playtesting (dash, collision, ranged attacks, enemy pressure, pause/Alt-Tab including real Alt-Tab, reset, all six dolls), committed as `cbafbdc`. Next: M4 run progression.

### M3 follow-up: pause-menu options and run timer — in progress, acceptance pending

- Director feedback received: Ian manually playtested M3 dash, collision, ranged attacks, enemy pressure, pause/Alt-Tab (real Alt-Tab included), reset and all six dolls, and accepted the core M3 mechanics. Remaining verification limitations below still apply.
- Approved follow-up scope (no M4, no tuning changes): pause menu gains Resume (continue run), Restart run (fresh run, same doll) and Change character (back to selection), reusing the existing `startRun`/`returnToSelection` flows with an explicit discard note; combat HUD gains a readable MM:SS TIME display driven by `world.elapsed` (consistent with spawn-pressure phases), frozen on pause/focus loss/game over and reset to 00:00 on every new/restarted run. HUD `elapsed` is floored whole seconds, so the HUD re-renders at most once per second.
- State clearing: restart/change reuse the same flows as game-over retry/switch (fresh world, cleared held keys/aim/queued dash, reset accumulator/timers/counters), so enemies, projectiles, weapon/reload, health/ammo, dash, spawn progression and pending actions are all reset. Browser focus safeguards untouched.
- Checks: `tests/systems/m3-followup.test.ts` (5 tests: MM:SS formatting, advance/freeze/reset, restart-from-pause freshness, selection switch, resume preservation); `tests/browser/m3-followup.spec.ts` (timer advances from 00:00, freezes in pause, resume preserves, restart keeps doll with fresh state, change character starts clean with another doll; `test-results/m3-pause-menu.png` inspected — three buttons, discard note and TIME 00:02 all readable).
- Next action: Ian reviewed and accepted the follow-up along with core M3 (committed as `cbafbdc`). M4 is now active; follow-up work is closed.

### M4 implementation and verification — 2026-09-06

- Scope: Ian's approved M4 subset — XP pickups/levels/choices, gun + equipment upgrades, one Sabrina evolution, Sabrina auto-pulse. No elite enemy (the design-doc elite reward is deferred; evolution requires gun rank 5 + Plated Vest rank 3 only), no M5/M6 systems, no permanent progression.
- Provisional tuning (`src/game/data/progression.ts`): pursuer 2 XP, ranged 4 XP; level need 3 + (level − 1) × 2, max level 20; gun ranks 1–5 at +15% damage each; equipment Plated Vest (+25 max HP, heals 25), Drum Magazine (+30% magazine, next reload), Trigger Unit (interval ×0.88, burst spacing unchanged), each max rank 3 with at most 3 distinct items; evolution Shockwave Barrage (Sabrina, every 4th volley: ×2 damage/knockback, +4 pellets); pulse every 6 s, radius 4, 8 damage + knockback; gems magnet 3.0 / pickup 1.0, cap 64 with overflow merged into the oldest gem; fallback Field Rations heals 30 and is always eligible. Dev-only `?devxp=N` grants N XP on selection-screen start (retries/selection never grant it).
- Systems: shared data-driven progression for all six dolls (`systems/progression.ts`); kills register exactly once with one gem drop (projectiles and pulse share `registerKill`); excess XP carries with one queued choice per level up to the cap; `offerChoices` always returns 3 distinct eligible picks (evolution/gun prioritized, equipment rotates deterministically, fallback floor); `applyChoice` never touches loaded ammo/cooldowns/bursts; new `levelup` run state freezes movement/combat/dash/spawn/elapsed; `chooseUpgrade` validates status and index, clears input, resumes with clamped delta after the last queued level; focus-loss/manual-pause guards unchanged around the upgrade screen; restart/retry/switch rebuild the world so all progression resets; HUD gains LV, XP bar, per-run max HP, derived magazine and EVO tag; green octahedron gems (larger for ranged drops).
- Defect found by tests: `stepPulse` computed Z-distance from the player to itself (X-only range check) — fixed, with a regression test.
- Checks performed: `npm run typecheck`, `npm run lint`, `git diff --check` passed; `npm run test` 97 passed in 7 files (26 new M4); `npm run build` passed (~1.11 MB / ~307 KB gzip, size warning only); `npm run test:browser` 16/16 passed in ~2.9 min (existing 13 + 3 M4: natural gem collection, level-up freeze/apply, dev-boosted evolution + restart reset). Screenshots `test-results/m4-gems.png`, `m4-levelup.png` (3 ranked choices + evolution progress line) and `m4-evolution.png` (LV 12, HP 175/175, EVO tag) inspected. Browser choice clicks need a settle wait after each applied pick (decisions raced the React commit); documented in the spec helper.
- Known limitations: provisional balance (evolution pace ~3–5 min estimated, needs director feel check); small gems in stills; pulse feedback is enemy hit-flash only; focus-loss coverage still synthetic; real zoom/DPR, other browsers, context recovery, reference-GPU performance remain manual. No accounts/cloud/permanent progression.
- Director feedback: M4 acceptance pending. Next action: Ian playtests progression with the README checklist. Stop after M4; do not mark accepted, commit, push or deploy.

### M4 follow-up: director playtest results and XP-bar layout — acceptance pending

- Director feedback received (2026-09-06): Ian playtested M4 — XP collection/drops good, level-up flow good, upgrades good, evolution works but needs further refinement (desired changes still need clarification; the evolution is NOT redesigned yet), restart/retry/character switching good.
- Approved HUD fix: the XP bar was absolutely positioned at a fixed `top: 86px`, breaking out of flow on wrap. It is now an in-flow flex row inside `.combat-hud` (`flex-wrap: wrap`, `gap: 8px 22px`, strip `flex: 1 1 100%`): full inner width respecting padding, 8 px spacing above via the row gap, no overlap with any label, correct when wrapped. No XP values, fill behavior or gameplay logic changed.
- Checks: `tests/browser/m4-hud.spec.ts` (2 tests) asserts the strip sits inside the HUD box, below every info cell with 6–12 px spacing, overlapping nothing, with unchanged aria/fill values — at 1280 px and 640 px widths. Screenshots `test-results/m4-hud-desktop.png` and `m4-hud-narrow.png` inspected. Full gates re-run: typecheck, lint, `git diff --check` passed; `npm run test` 97 passed; `npm run build` passed; `npm run test:browser` 18/18 passed.
- Next action: Ian clarifies the evolution refinement, then reviews this HUD fix. Do not mark accepted, commit, push or deploy.

### M4 follow-up: survival layout, arena and pacing — acceptance pending

- Approved scope: full-window game view, larger placeholder arena, initial pressure tuning, and progression balance for the denser curve. M4 core, the XP-bar fix and the evolution-refinement discussion keep their existing states; evolution mechanics unchanged.
- Layout: the arena view fills the whole browser viewport (`inset: 0`, no page scroll, no fullscreen needed). The slim top bar (Pause stays usable) and the wrapping bottom control bar overlay the scene with translucent backgrounds; the combat HUD stays overlaid top-right below the bar with its full-width XP row and 8 px spacing intact. Selection scrolls internally with top-bar clearance; pause/level-up/game-over dialogs center over the full view. Camera style untouched (fixed tilt, 45% follow, cursor-ground projection from the live canvas rect), so aiming is unaffected by size or aspect.
- Arena: 24 × 24 with one block → 40 × 40 bounded floor with four scattered blocks (east block keeps its M2/M3-tested geometry; north horizontal, west vertical, south-east vertical; open lanes between them). Camera framing unchanged (half-height 14) to preserve doll/enemy/warning/projectile readability. Spawn points moved to eight edge positions; spawns prefer points 16+ units away and never land within 8; capped populations still never bank.
- Before → after tuning:

  | Setting | Before | After | Reason |
  | --- | --- | --- | --- |
  | Arena | 24 × 24, 1 block | 40 × 40, 4 blocks | Room to maneuver and retreat |
  | Max enemies | 8 | 20 | Density for the bigger space |
  | Phases | 3.5 s/6/1r @0 s; 3.0 s/6/2r @15 s; 2.6 s/8/2r @40 s | 3.0 s/6/1r @0 s; 2.4 s/10/2r @20 s; 2.0 s/14/2r @60 s; 1.6 s/18/2r @120 s | Gradual pressure supporting M4 builds; ranged capped at 2 for readable warnings |
  | Spawn distance | 6 | 8, far points preferred | Bigger arena, no beside-the-player spawns |
  | Level need | 3 + (level−1) × 2 | 5 + (level−1) × 3 | ~2.5× kill throughput → levels every ~30–45 s, evolution ~2–3 min; drops unchanged |
- Checks performed: typecheck, lint, `git diff --check` passed; `npm run test` 98 passed in 7 files (incl. a 240 s unit soak asserting live phase caps on every step — caps never exceeded, ranged ≤ 2, gems ≤ 64); `npm run build` passed; `npm run test:browser` 19/19 passed (incl. a 65 s+ sustained browser run: LV 7, 16 kills at 01:05, up to 10+ alive, ranged ≤ 2, no errors). Measured soak bot: evolution at ~116 s (level 9, 48 kills), survived all 240 s; the 240 s sim runs in ~1 s of Node time, so no simulation-side bottleneck — rendering is instanced batches, but only software-GL browser checks were run. Screenshots inspected: `m4-pressure.png` (full-window density), `m4-hud-desktop/narrow.png`, selection at both widths (temp captures, since removed). No polished art, new enemy types, kits, saves, or M5/M6 work.
- Known limitations: provisional pacing needs human feel review (level-up frequency ~1 per 15–30 s early); real zoom/DPR, other browsers, context recovery, reference-GPU frame times and ultrawide/portrait extremes remain manual. After choosing upgrades, auto-fire waits for the mouse to move over the canvas again (pre-existing pointer-clear behavior).
- Next action: Ian playtests pacing with the checklist below. Do not mark accepted, commit, push or deploy.

### M4 follow-up: run chrome without header/footer — accepted and recorded

- Director feedback received: Ian tested the header/footer cleanup and it looks good. Acceptance recorded; no gameplay, camera, spawn, XP, evolution, or input changes were part of this cleanup.

- Director feedback received: the larger arena is a good baseline (further environment work waits for M5); density and spawn pressure feel good after one minute. Approved UI cleanup: hide the application header and the bottom control-instructions bar during runs (restored in selection), keep a compact standalone Pause overlay top-right, and move the control instructions into the pause dialog.
- Changes (`App.tsx`, `styles.css`, `PauseDialog.tsx`; no gameplay/camera/spawn/XP/evolution/input changes): header renders only in selection; the footer is removed and its instructions live in a compact `control-hints` section of the pause dialog (dash row only in combat); Pause is an absolute top-right overlay keeping its label, Escape hint and playing-only availability; combat HUD moves up below it with the full-width XP row and spacing intact; scene label/note repositioned to the freed corners. Nothing invisible blocks mouse input (no gaps, no reserved panels).
- Checks: new `tests/browser/m4-chrome.spec.ts` (2 tests) asserts header hidden and footer absent through start, pause/resume, level-up, game over, retry and return-to-selection; Pause/HUD bounding boxes never overlap; XP row stays below all cells; control hints visible; screenshots `m4-chrome-desktop/narrow/pause(-narrow).png` inspected. Full gates: typecheck, lint, `git diff --check` passed; `npm run test` 98 passed; `npm run build` passed; `npm run test:browser` 21/21 passed.
- Test-setup notes (no game defects): corner-aim death setups no longer starve fire on a full-window canvas — death tests now park on the Pause overlay (canvas `pointerleave` clears aim) and walk into contact; the Tololo focus assertion now polls because the canvas snapshot trails React by a frame; the pressure peak assertion pairs live-count (≥7) with kill throughput (≥12) since lethal builds suppress the field.
- Next action: UI cleanup accepted above; evolution refinement still awaits clarification. Do not mark M4 accepted, commit, push or deploy.

### M4 follow-up: selection-screen palette — pending visual acceptance

- Provisional palette (GFL2 orange/charcoal/warm-white references; images used as palette reference only, no official logo or icon added): background #121316, card surface #1E2024, borders #3B3E44, primary accent #FF7800, hover accent #FF952E, main text #F4EFE7, secondary text #B8B5AF, dark on-accent text #231303.
- Changes (`styles.css` only; no layout, content, behavior, or doll-color changes): named `--menu-*` variables on `.app-shell`, consumed only by `.topbar` and `.selection-screen` rules — combat HUD, arena, projectiles, warnings, gems and dialogs are untouched. Charcoal background, neutral cards, orange Start button with dark text, orange selected border/tint/label, muted unselected labels, warm-white/muted-gray text, distinct doll placeholder colors kept. Hover is a surface lift only (no orange border) so it stays distinct from selection; global keyboard-focus outline retained; disabled states defined.
- Checks: behavior verified with a temporary spec (since removed) covering all six selected states with SELECTED labels, Tab/Enter keyboard selection, Start run, and return to selection at 1280 px and 640 px — all passed. Screenshots `menu-desktop.png`, `menu-narrow.png` (folded into `m4-chrome.spec.ts` for regeneration) inspected, plus per-doll captures (temp, since removed). Full gates re-run: typecheck, lint passed; `npm run test` 98 passed; `npm run build` passed; `npm run test:browser` 21/21 passed. No per-color unit tests (redundant by instruction). Contrast is by construction: warm white / muted gray on charcoal (~12:1 / ~7:1), dark text on orange (~8:1), orange labels on charcoal (~8:1).
- Next action: Ian's visual review. Evolution refinement remains unresolved; do not mark M4 accepted, commit, push or deploy.

### M5 environment blockout — in progress, acceptance pending

- Authorization: first bounded-environment task only. M4 evolution refinement remains unresolved and is not redesigned or marked fixed here.
- Planned/implemented map decision: expand the former 40 x 40 arena to a finite 120 x 96 (X x Z) stage. At 6 u/s, its 120-unit east-west crossing is approximately 20 seconds and spans multiple camera footprints. The starting combat space remains at world centre.
- Environment: original procedural ground/concrete route bands, visible dark perimeter fence with restrained hazard stripe, broad landmark props, and a near-start north-west checkpoint around X -18 / Z -16. The checkpoint uses low concrete collision cover; its fence, crates, barrels and markings are explicitly decorative. No screenshot, UI, enemy, or reference image is loaded at runtime.
- Spawning: phase pacing/live caps/ranged limits are unchanged. Distributed valid spawn positions follow the player across the map (12-unit exclusion, 22-unit preferred distance). An enemy remaining more than 52 units away for 10 seconds despawns without a kill or XP drop, preventing abandoned groups from holding the live cap; ordinary defeat/XP behavior is unchanged.
- Blender status: the available session tool inventory has no Blender MCP endpoint. No Blender scene, `.blend`, generation/export script, GLB, or runtime asset has been created or claimed. `src/render/Arena.tsx` and `src/game/data/arena.ts` are an intentional procedural fallback, not a replacement for the requested reusable Blender kit/export workflow.
- Verification: `npm run typecheck`, `npm run lint`, `npm run test` (98 tests in 7 files), `npm run build`, and `git diff --check` passed after the map change. The production build is 1.118 MB minified / 307.79 KB gzip; Vite's existing chunk-size warning remains. The full Playwright command was started twice but this environment terminated its runner/session before it returned a complete summary, and no desktop/browser target was available for a manual visual pass; neither browser verification nor real-GPU performance is claimed. M5 acceptance remains pending; no commit, push or deployment.

- Milestone and date:
- Changes:
- Build/typecheck/lint/test results:
- Browser and machine:
- Manual steps actually performed:
- Performance scene/counts/results if relevant:
- Known issues and deferred checks:
- Director feedback, if actually received:
- Commit, if created:
- Next task:

### M5 reusable checkpoint environment kit — in progress, acceptance pending

- Scope: first detailed checkpoint section only. The 120 x 96 stage, central starting area, collision/spawn systems, and remaining procedural landmarks/blockout were preserved. M4 evolution refinement remains unresolved and unchanged.
- Blender assets: connected Blender 5.2 MCP created `M5_EnvironmentKit` without modifying the pre-existing Cube, Light, or Camera. The dedicated collection contains `ENV_GroundConcrete_Module`, `ENV_ConcreteBarricade_Low`, `ENV_FencePanel_Plated`, `ENV_SupplyCrate`, and `ENV_Barrel`, with nine original PBR materials. Source: `assets-source/environment-kit/m5_environment_kit.blend`; repeatable source/export script: `assets-source/environment-kit/generate_environment_kit.py`; GLBs: `public/assets/environment-kit/` (11.8–30.0 KB, 92.8 KB total). No external art, textures, characters, or reference screenshots are shipped.
- Integration: `src/render/Arena.tsx` loads the five GLBs through `GLTFLoader`. Checkpoint fence/crate/barrel procedural visual duplicates were replaced. The ground/concrete tile is placed at the checkpoint. The three checkpoint barricades are GLB instances scaled and rotated from their existing collision rectangles; all other collision-cover rendering and decorative remote map props remain procedural. Fence/crate/barrel remain decorative.
- Technical checks: `npm run typecheck`, `npm run lint`, `npm run test` (98 passed in 7 files), `npm run build`, and `git diff --check` passed. Production output: 1,163.94 KB minified / 321.58 KB gzip; existing chunk-size warning only.
- Browser checks: headless installed Chrome using software WebGL/SwiftShader loaded the production build with no page errors. Focused walkthrough `scripts/verify-m5-browser.mjs` reached the checkpoint approach (about X -8.1 / Z -12.7 in the recorded run), visually showed the imported fence, crates, barrels and barricades, and was blocked by the matching eastern barricade collision. The helper verifies that Pause changes the snapshot status to `paused`; screenshots are ignored outputs `test-results/m5-checkpoint-playing.png` and `m5-checkpoint.png`. A full 21-test Playwright command was interrupted by the execution session after several passing scenarios and did not return a final summary; it is not counted as a complete suite pass.
- Limitations: no targetable interactive browser was available through the computer-use connector, so no human/manual walk to every boundary/corner, resize, dash, projectile, level-up, retry or character-switch pass was completed in this session. The focused browser run is software-rendered, not a real-GPU performance measurement. M5 is not accepted; no commit, push, or deployment was performed.
- Next action: Ian reviews the checkpoint at X -18 / Z -16 using the M5 checklist, especially readability, collision alignment, and combat visibility; then direct any art/layout refinement.

### M5 checkpoint refinement — axis fix and completed verification — 2026-09-06, acceptance pending

- Authorization: Ian's task-scoped authorization to finish the interrupted M5 checkpoint refinement (verification, necessary fixes, documentation). No scope expansion: one checkpoint section in the 120 x 96 map, six dolls/gameplay/progression untouched, M4 evolution refinement still unresolved, no commit/push/deploy, M5 not marked accepted.
- Prior run recovered: the interrupted full browser suite had actually completed at 22:57 with 17/21 passing and 4 failures (`test-results/.last-run.json`, `m5-browser-suite.log`). No duplicate suite was launched while one ran; the original logs were reused as diagnosis evidence. No reliable baseline before/after captures existed (no `m5-checkpoint*.png` on disk; Playwright clears `test-results/` at each run start), so none are claimed.
- Concrete defect found by GLB audit (isolated background Blender `D:\Blender\blender.exe --background`, live scene untouched): `generate_environment_kit.py` authored all sizes/locations/rotations as (x, up, forward), but Blender is (x, forward, z-up). Every part was mis-oriented: ground slab was a 6 m vertical wall, barricade squat/buried, fence lying flat, barrel parts offset horizontally with halves below ground, preview instances placed below ground. Fixed the generator (sizes/locations swapped to (x, y_forward, z_up), ground-decal yaw moved to Z, fence/crate brace tilts moved to Y, preview placements grounded), backed the pre-fix `.blend` to temp, rebuilt via live Blender MCP, saved `m5_environment_kit.blend`, re-exported the five GLBs (ground 20,404 B, barricade 17,028 B, fence 35,572 B, crate 16,612 B, barrel 30,196 B; 119,812 B total).
- Export audit after the fix (background Blender re-import + stdlib GLB JSON parse): each GLB contains exactly its intended asset under a single `ENV_*` root (ground 9+root, barricade 7+root, fence 15+root, crate 7+root, barrel 5+root meshes), zero `PREVIEW_*` leaks (preview source copies correctly excluded by selection export), embedded PBR materials only, no images/textures/animations/skins. Live-scene probe confirms grounded placement (Ground_Slab z 0–0.12, Barrier_Main z 0–0.92, Barrel_Body z 0–0.94, Fence_LowerPlate vertical). Scene still 104 objects; Cube/Light/Camera and all preview content preserved. Note: `bpy.data` holds ~105 zero-user orphan objects from repeated kit rebuilds (old part generations); they are unlinked, never exported (selection-only export verified clean), and were left in place rather than purged to avoid touching any stashed user data.
- Browser-scenario maintenance for the 120 x 96 map (game mechanics unchanged; test scenarios assumed the old 40 x 40 map): `playground.spec.ts` boundary 19.6 → 59.6 with a 20 s budget, pause-held assertion polls up to 5 s (diagnostic snapshot is written by the render loop, which lags a frame under SwiftShader+shadows; the game clears inputs synchronously); `m3.spec.ts` telegraph scenario now waits for full canvas size before aiming, aims at the nearest enemy, walks toward the ranged enemy and drains level-up dialogs (far spawns + slow ranged walkers made the stationary scenario unwinnable); `m4-pressure.spec.ts` aims at the nearest-to-centre enemy and sustains to elapsed 80 s for the same kill-stream engagement budget on far spawns. No gameplay, tuning, or production-code changes in these edits.
- Final verification on the production build (dist 1,164.20 KB minified / 321.68 KB gzip, existing chunk warning only): `npm run typecheck`, `npm run lint`, `npm run test` 98/98 passed; `scripts/verify-m5-browser.mjs` passed (checkpoint approach X -8.1 / Z -12.6, blocked by the eastern barricade collision, pause verified, zero page errors; `test-results/m5-checkpoint-playing.png` + `m5-checkpoint.png` inspected — upright grounded kit, readable hazard/details, consistent shadows, doll/enemy/aim ring/HUD unobscured); full `npm run test:browser` 21/21 passed in ~5.2 min (headless Chrome 152, SwiftShader software WebGL, Windows x64). `.last-run.json` status passed.
- Remaining limitations: software rendering only — real-GPU frame timing, physical Alt-Tab, zoom/DPR, other browsers, and human feel still need Ian's manual pass; the verify-m5 hover leaves aim null on its first frame (script race, not a game defect — the suite proves aiming); test captures are ignored artifacts regenerated per run. The rest of the 120 x 96 stage stays procedural blockout.
- Next action: Ian's in-browser playtest at the checkpoint per `docs/PLAYTEST_CHECKLIST.md` (M5 section) and art-direction verdict. Do not commit, push, deploy, or mark M5 accepted.

### M5 checkpoint composition — cohesive area, pattern fix, new assets — acceptance pending

- Authorization: Ian's task-scoped authorization for this checkpoint-composition task (Blender assets, generator, runtime placement, docs; save .blend; export; verify). No MMD imports, no new enemies/bosses, no rest-of-stage work, no commit/push/deploy, M5 not accepted, M4 evolution still unresolved.
- Environment verified first: git state matches the prior session plus this task's edits; live Blender MCP read-only probe confirms the intended `m5_environment_kit.blend` is loaded, saved (not dirty), kit collection intact, Cube/Light/Camera preserved. MCP configuration untouched. Reference screenshots used as direction only: `src/Stage Screenshots/Screenshot (145).png`, `(146).png` (checkpoint), `(118).png` (industrial details).
- Diagonal-pattern cause found by experiment, not assumption: dense hatching on all GLB up-faces. Blender meshes verified clean (plain 8-vert boxes, flat shading, no custom normals, no degenerate faces). Bias 0.12/0.35, light mirroring, and 256 px shadow-map trials left it unchanged; `castShadow=false` removed it; `normalBias=2.0` removed it along with all shadows. Root cause: shadow-map acne — 1024 px map over the ±42 m ortho frustum (~8 cm texels) plus SwiftShader software depth precision, needing a larger-than-usual offset. Fix kept minimal and unmasked: `shadow-bias -0.0005`, `shadow-normalBias 0.8` on the key directional light (`src/render/PlaygroundScene.tsx`). Verified: smooth surfaces with tight grounding shadows; no blur/darkness/post-processing masking.
- Composition (all within the checkpoint vicinity, X −30…−2 / Z −25…−8): second ground tile forms a 6×12 concrete apron joining the checkpoint to the existing road; west/east barricades keep their collision rects and now read as an inspection entrance with a 2.5 m gap; two new `ENV_Gatepost` posts (concrete post, cap, placard + hazard stripe, facing the southern approach) flank the gap clear of the walk line; west fence line extended one panel as the outer edge; crates/barrels regrouped into a west supply corner beside the route; four `ENV_DebrisSmall` scatters on the perimeter, off lanes; existing route marker kept. New assets reuse existing materials (no new textures).
- Gameplay contracts: zero collision changes (all 10 `COMBAT_OBSTACLES` footprints byte-identical in the diff); map size, spawn pacing, XP/weapons/upgrades/evolution/run lifecycle unchanged; new decor is decorative and non-colliding; gateposts thin and outside the walk line; no bottlenecks or XP pockets (strand-despawn and magnet rules untouched). Walkthrough re-verified deterministic blocking at the same endpoint (X −8.1).
- Reproducibility: generator updated (gatepost/debris builders, 7 preview slots), `.blend` saved, all 7 GLBs exported (150,480 B total; the 5 unchanged assets byte-identical except ±4 B datablock renames). Audit (isolated background Blender + GLB JSON parse): single `ENV_*` roots, zero `PREVIEW_*` leaks, no images/textures/animations; live-scene probe confirms grounded Z-up placement (post 0–1.7, placard z 1.15 facing +y, debris ≤0.18). Kit is 59 linked objects; pre-composition `.blend` backed up to temp.
- Verification on the final production build: `npm run typecheck`, `npm run lint`, `npm run test` 98/98, `npm run build` passed; `verify-m5-browser.mjs` passed (approach, pause, zero page errors); full `npm run test:browser` 21/21 passed (~5.1 min, headless Chrome 152/SwiftShader). Before/after captures at the same gameplay camera: `test-results/m5-checkpoint-before.png` (hatched demo scatter) vs `m5-checkpoint-playing.png` + `m5-checkpoint.png` (smooth composed entrance). Close gameplay view and road-joining approach both inspected.
- Remaining limitations: software rendering only (real-GPU check may show even less acne; the 0.8 offset stays safe there); no human playtest of the new layout; rest of the map remains blockout; tiny gem/projectile shadows may be softer under the larger normalBias (garnish only).
- Next action: Ian's review of the composed checkpoint per the checklist below. Do not mark M5 accepted, commit, push, or deploy.

### M5 task 2 — map-wide environment expansion — acceptance pending

- Authorization: Ian's task-scoped authorization (Blender/export/code/docs/Git-tracking cleanup; no MMD imports, bosses, evolution redesign, or M6; stop for review without committing, pushing, deploying, history rewrites, or marking M5 accepted). Baseline preserved: 120×96 map, camera scale, checkpoint anchor, 7-asset kit, corrected axes/export isolation, 98 unit + 21 browser tests green.
- Blender verified read-only first: intended `m5_environment_kit.blend` loaded, saved (not dirty), 59 kit objects, Cube/Light/Camera preserved; MCP configuration untouched.
- Layout (checkpoint anchor kept intact): central staging yard stays open around its 4 covers with a concrete road patch, faded marker, and an eastern barrel pair; E–W road dressed with worn patches and lane markers; storage/service yard east (grouped crates/barrels on a pad, fenced north edge, wall back, pipe rack, open south); west works area (rocks, wall, rack, barrel); south field (pad, barrels, crate, L-corner wall); weathered perimeter (north/south edge fences, north edge wall landmark, corner rocks, off-lane debris). Lanes stay broad; no enclosures, bottlenecks, or repeated identical clusters.
- New reusable assets (2, existing materials only): `ENV_WallSection` (16,140 B, 5 parts) and `ENV_PipeRack` (30,068 B, 5 parts, verified horizontal pipe orientation in export). Kit total: 9 GLBs, 196,692 B. All other assets byte-identical on rebuild.
- Gameplay contracts: zero collision edits (all 10 obstacle footprints identical in diff); all 10 covers now render as barricade-kit instances scaled to their rects (procedural gray boxes removed, visuals match collision everywhere); every new piece decorative and non-colliding; fences/walls outside walk lines; spawn points, pacing, caps, no-banked-spawn, magnet, and stranded cleanup untouched. Deterministic walkthroughs reached every area and the W/N/SE extremes at full HP with zero page errors.
- Rendering: 9 cached GLBs instanced ~65 times (shared geometry/materials, no new textures); shadow frustum widened to ±62/±50 with a 2048 map so coverage spans the whole map (~6 cm texels, finer than before); bias/normalBias kept — no acne on any capture, drop shadows tight map-wide. Suite wall-time unchanged (~5.1 min). Real-GPU frame cost not measured; simulation soak speed is not a GPU claim.
- Reference-file cleanup prepared for the next commit (not committed/pushed): found a prior partial staging (46 screenshots untracked, `.gitignore` with a wrong `/Character MMD/` rule); corrected the rule to the exact `src/Character MMD/`, ran `git rm --cached -r` on the 10-dir/241-file MMD tree (worktree preserved), staged `.gitignore`. Index now drops 46 screenshots + 241 MMD files; all 46 PNGs and 241 MMD files verified present locally and readable (Screenshot 145, Sabrina PMX spot-checked). Earlier commits (incl. `d466ad0`, in sync with `origin/main`) still contain them — no history rewrite. GLBs, `.blend` files, and generator untouched by the cleanup (narrow rules only).
- Verification on the final build: typecheck, lint, 98/98 unit, build passed; `verify-m5-browser.mjs` passed; full `test:browser` 21/21 passed; area tour (staging/checkpoint/west+W-boundary/storage/south/SE-corner/north-boundary) with HP 100 throughout. Captures: `m5-area-{staging,storage,southfield,westworks,checkpoint,northedge,southeast,northfield}.png` plus `m5-checkpoint-playing/before.png`. No top-down overview mode exists — coverage is via the follow-camera tour (documented gap).
- Remaining: Ian's manual/reference-GPU pass; tile edges still read as rectangles up close (accepted blockout-grade transition); south-field framing limited by the 45%-follow camera (content verified via data + traversal); M4 evolution still unresolved.
- Next action: Ian's playtest per the checklist below. Do not mark M5 accepted or complete, commit, push, deploy, or rewrite history.

